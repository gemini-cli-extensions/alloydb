{
  "rules": [
    {
      "pattern": "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\([\\w\\s-]+\\))?!?: .+$",
      "error": "The pull request title must adhere to the Conventional Commits specification. (e.g., 'feat: add a new feature' or 'fix(app): correct a bug')"
    }
  ]
}
